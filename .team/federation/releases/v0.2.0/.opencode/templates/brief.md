# <run>-T<id> — <title>

Owner: pm. Copy to `.team/tasks/<run>-T<id>/brief.md`. The run id (`YYYYMMDD-<objective-slug>`) is created by pm at run start; task ids are run-scoped.

## Goal

One or two sentences: what the user-visible outcome is.

## Scope

- In scope: ...
- Out of scope (explicit): ...

## Acceptance criteria

1. <observable, testable statement>
2. <…>

## Dependencies / constraints

- Depends on: T<n>
- Constraints / gotchas for designer:

---

**Handoff — pm**
- Status: READY_FOR_DESIGNER
- Changed: task created; criteria set
- Next: **designer** must produce `spec.md` including a test plan covering each criterion
- Notes: none