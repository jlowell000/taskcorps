# Team runtime workspace — NOT tracked. All .team/ state is local/ephemeral; it is never
# committed (survives only on this machine, not clones). The baseline's canonical files live
# in .opencode/ and the release snapshots are regenerated, not versioned in git.
.team/

# Editor / OS noise
.DS_Store
*.swp

# Python
__pycache__/
*.pyc





# Taskcorps agent files — NOT tracked (tooling; remotes may not want them)
AGENTS.md
CLAUDE.md
opencode.json
.opencode/
